# WEB_FACTORY APP

Controlled local base for the WEB_FACTORY operational cabin.

## Runtime

- `npm run build`
- `npm start`
- Health: `/api/health`
- Provenance: `/api/provenance`

## Governance

This app does not deploy client assets, buy domains, call external builders, or report live analytics until approved integrations and credentials exist.
